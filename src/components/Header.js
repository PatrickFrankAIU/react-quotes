// Header component for the Quote React Demo
import React from 'react';

function Header() {
  return (
    <header>
      <h1>Quote React Demo</h1>
      <p>Select a topic and count to fetch quotes from our server</p>
    </header>
  );
}

export default Header;